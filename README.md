# TkinterProject  

A collection of small GUI applications built using Python’s `tkinter` library.  
This project currently includes:
- `Calculator.py` – a basic calculator GUI  
- `loginForm.py` – a login form application  
- `address_book.db` – a sample SQLite database supporting the address book (if applicable)  

---

## 🧰 Features  
- Lightweight Python GUI applications, easy to understand and extend  
- Demonstrates usage of `tkinter` widgets, layout managers, event handling  
- Sample database file (`address_book.db`) included for learning purposes  

---

## 📦 Requirements  
- Python 3.x  
- Standard library only (no external dependencies)  
- (Optional) SQLite3 if you wish to inspect or extend `address_book.db`  

---

## 🚀 Getting Started  
1. Clone the repository:  
   ```bash
   git clone https://github.com/BhupatiNadar/TkinterProject.git
   cd TkinterProject
